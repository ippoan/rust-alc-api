[package]
name = "alc-auth-jwt"
version = "0.1.0"
edition = "2021"

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(coverage)'] }

# JWT 発行・検証の auth-worker 移管 (#479 PR-3) で共有定数のみの crate になった
# ため依存ゼロ (jsonwebtoken / serde / sha2 / chrono / uuid は撤去済み)。
[dependencies]
